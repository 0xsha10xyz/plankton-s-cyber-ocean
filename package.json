{
  "name": "plankton-s-cyber-ocean",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo: frontend (Vite + React) and backend (Express + TypeScript)",
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:all": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=frontend && npm run vercel-build",
    "build:backend": "npm run build --workspace=backend",
    "vercel-build": "node scripts/copy-dist.cjs",
    "preview": "npm run preview --workspace=frontend",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspace=frontend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "overrides": {
    "rimraf": "^5.0.5",
    "@solana/kit": "^6.8.0",
    "@base-org/account": "^2.5.5",
    "ox": "0.14.20"
  },
  "dependencies": {
    "@privy-io/node": "^0.16.0",
    "ox": "^0.14.20"
  }
}
