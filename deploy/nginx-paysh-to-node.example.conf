# If nginx proxies /api/paysh directly to Corbits, the Node adapter never runs and `pay` keeps seeing
# solana-mainnet-beta + Corbits resource URLs. Proxy to your Express port instead (see docs/DEPLOYMENT.md).
#
#   location /api/paysh/ {
#       proxy_pass http://127.0.0.1:3000;
#       proxy_http_version 1.1;
#       proxy_set_header Host $host;
#       proxy_set_header X-Forwarded-Host $host;
#       proxy_set_header X-Forwarded-Proto $scheme;
#       proxy_set_header X-Real-IP $remote_addr;
#   }
