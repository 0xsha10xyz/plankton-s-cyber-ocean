# Root-level env templates were removed with the legacy standalone signal poller.
# Copy and edit the workspace you run:
# - frontend/.env.example  →  frontend/.env
# - backend/.env.example   →  backend/.env
# - api/.env.example       →  Vercel / local api/
