{
  "buildCommand": "npm install && npm run build && if [ -d frontend/dist ]; then cp -r frontend/dist .; fi",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
