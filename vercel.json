{
  "buildCommand": "npm install && npm run build:backend && npm run build && if [ -d frontend/dist ]; then cp -r frontend/dist .; fi",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
