{
  "buildCommand": "npm run build:backend && npm run build && npm run vercel-build",
  "outputDirectory": "dist",
  "functions": {
    "api/[[...path]].ts": {
      "runtime": "nodejs20.x",
      "includeFiles": "api/**"
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
