{
  "buildCommand": "npm run build && npm run vercel-build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/agent/(.*)", "dest": "https://api.planktonomous.dev/api/agent/$1" },
    { "src": "/api/usage/(.*)", "dest": "https://api.planktonomous.dev/api/usage/$1" },
    { "handle": "filesystem" },
    { "src": "/api/config", "dest": "/api/health?mode=config" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "headers": [
    {
      "source": "/api/config",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "s-maxage=3600, stale-while-revalidate=86400"
        }
      ]
    }
  ],
  "experimentalServices": {
    "frontend": {
      "entrypoint": "frontend",
      "routePrefix": "/",
      "framework": "vite"
    },
    "backend": {
      "entrypoint": "backend",
      "routePrefix": "/_/backend"
    }
  }
}
