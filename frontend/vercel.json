{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.well-known/vector-verify", "dest": "/api/vector-verify" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
