# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
backend/data

# Environment and secrets (never commit — wallet and API safety)
.env
.env.local
.env.development
.env.production
.env.*
!.env.example
!frontend/.env.example
!backend/.env.example
*.pem
*.key
*secret*
*credentials*
wallet.json
keystore*
mnemonic*
*.keystore

# Accidental env backups / dumps (never commit real values)
.env.backup
.env.*.backup
*-firebase-adminsdk-*.json
serviceAccount*.json

# User/data that must not be in git
backend/data
backend/data/*
**/connected-wallets.json

# Legacy Vercel Express bundle (no longer produced by copy-dist; safe to delete locally)
vercel-express-bundle

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
